# 个人资料页 · 效果试用版

这是用于挑选功能的预览分支，不会替换 `main` 上的个人资料。

| 效果 | 实现 | 试用方法 |
| --- | --- | --- |
| 打字机 | SVG 分步揭示三句话 | 看封面下方，完整循环 15 秒 |
| 昼夜封面 | picture + 媒体查询 | 随浏览器颜色偏好选择，或展开封面对比 |
| 动态作品卡 | SVG 9 秒流光 | 点击卡片进入微墨项目 |
| 最近文章 | RSS / Atom 解析并更新两份 README | 当前是演示笔记，填写 `content/sources.json` 的 `feed_url` 后接入真实内容 |
| 折叠书架 | details | 点击分类展开；真实书单待补充 |
| 贡献图动画 | 真实公开日历 + SVG 小蛇 | 42 秒循环，浅深色自适应 |
| 中英文 | 两份 README 互链 | 点击页面顶部 English / 简体中文 |

所有动效尊重系统“减少动态效果”偏好。贡献图采用 GitHub 提供的 0–4 活跃等级，不把等级当成提交数量。

## 自动刷新

`python3 scripts/refresh.py` 获取公开贡献日历；配置了 `feed_url` 时，按源的条目顺序选前三篇。

工作流每天 02:23 UTC 刷新，只在默认分支上的 schedule 会自动运行。预览分支首次推送会验证生成器并刷新一次；没有改动时不提交。采用到主页后才会开始每日刷新。

RSS / Atom 接入失败时工作流会报错，旧的公开文件保留。没有源时保留明确标注的示例。公众号草稿、私有配置和个人密钥不会被此脚本读取。

## 维护

- 封面：`assets/header-dark.svg`、`assets/header-light.svg`。
- 打字机与卡片：`scripts/build_design.py`。
- 真实数据与文章列表：`scripts/refresh.py`。
- 书架：直接编辑 README；不要把示例误作真实阅读记录。
- 预览的七项效果可以分别保留或移除。
